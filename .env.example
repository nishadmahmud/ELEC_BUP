# Copy to .env and fill in. Never commit .env or real secrets.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# Optional: leave unset to retry the same primary model. Set only if you want a different fallback.
# OPENAI_BACKUP_MODEL=gpt-4o-mini
PORT=8000
