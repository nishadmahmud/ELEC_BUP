# Copy to .env and fill in. Never commit .env or real secrets.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
# Optional backup model if primary fails after retry
OPENAI_BACKUP_MODEL=gpt-4o
PORT=8000
